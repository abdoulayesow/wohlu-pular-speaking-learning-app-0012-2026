/** App-wide shared constants */

export const APP_NAME = "Wohlu";
export const APP_TAGLINE = "Learn Pular together, as a family.";

/** Minimum touch target size in pixels (WCAG / mobile best practice) */
export const MIN_TOUCH_TARGET_PX = 48;

/** Spaced repetition defaults (simplified SM-2) */
export const SM2_INITIAL_EASE = 2.5;
export const SM2_MIN_EASE = 1.3;
export const SM2_INITIAL_INTERVAL_DAYS = 1;

/** Max phrases per review session */
export const MAX_REVIEW_PHRASES = 10;
